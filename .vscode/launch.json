{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/src.exe",
            "args": [],
            "preLaunchTask": "Odin: Build",
            "cwd": "${workspaceFolder}",
            "sourceLanguages": ["odin"]
        }
    ]
}